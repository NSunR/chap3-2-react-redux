//action values
const changeValue = "CHANGE";

//Action Creator

//initial
const initState = {
  num: "",
  comment: "",
};

//reducer
const form = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

//export default reducer

export default form; // 여기
